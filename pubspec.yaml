name: whats_app
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

  # firebase
  firebase_core: ^2.16.0
  firebase_storage: ^11.2.7
  firebase_auth: ^4.10.0
  
  # state management
  flutter_bloc: ^8.1.3
  get_it: ^7.6.4
  hydrated_bloc: ^9.1.1
  equatable: ^2.0.5
  formz: ^0.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  generate: true
  uses-material-design: true
  fonts:
    - family: Droid
      fonts:
        - asset: assets/fonts/Droid_Arabic.ttf
  assets:
    - assets/data/
    - assets/images/
